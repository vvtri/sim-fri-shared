export enum NotiType {
  COMMENT = 'COMMENT',
  LIKE = 'LIKE',
}

export enum NotiTemplateType {
  COMMENT = 'COMMENT',
  MENTION = 'MENTION',
  LIKE = 'LIKE',
}

export enum NotiSenderType {
  USER = 'USER',
}
